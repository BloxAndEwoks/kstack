{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "kstack": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "exec node \"${PLUGIN_ROOT:-.}/mcp/review-state.mjs\""
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
