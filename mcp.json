{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "kstack": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": [
        "-c",
        "exec \"${PLUGIN_ROOT:-.}/mcp/run.sh\""
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
