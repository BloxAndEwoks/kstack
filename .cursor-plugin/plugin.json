{
  "name": "kstack",
  "version": "1.1.0",
  "description": "The bookended PR loop, portable: task -> worktree -> commits -> review -> PR -> merge-ready handoff. Router + playbooks + the QA finding protocol, plus an MCP server that gives any agent the review-state tools the cloud harness has.",
  "author": {
    "name": "keivan"
  },
  "license": "MIT",
  "keywords": [
    "pr-loop",
    "review",
    "agent-workflow",
    "qa"
  ],
  "repository": "https://github.com/BloxAndEwoks/kstack",
  "hooks": "./hooks/cursor-hooks.json",
  "mcpServers": {
    "kstack": {
      "command": "/bin/sh",
      "args": [
        "${CURSOR_PLUGIN_ROOT}/mcp/run.sh"
      ],
      "cwd": "${CURSOR_PLUGIN_ROOT}"
    }
  }
}
