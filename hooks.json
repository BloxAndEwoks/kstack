{
  "description": "kstack session hooks — thin and non-fatal. SessionStart prints the unit's ground-truth context; Stop watches the PR for new comments, newly-failing checks, and pending findings so review traffic surfaces without being summoned.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/mcp/review-state.mjs --print-context 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/mcp/review-state.mjs --watch 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
