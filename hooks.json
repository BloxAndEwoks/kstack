{
  "description": "kstack session hooks — thin and non-fatal. SessionStart prints the unit's ground-truth context (repo root, branch, BASE, dirty state) so the loop never reconstructs it from memory.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/mcp/review-state.mjs --print-context 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
